version: "3.8"

services:
  credit-copilot:
    build: .
    container_name: credit-ai-copilot
    ports:
      - "80:80"
    env_file:
      - .env
    volumes:
      - ./data:/app/data
    restart: unless-stopped
